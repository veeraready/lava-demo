metadata:
  name: test-run-docker
  description: Running test arbitrary code on worker using docker
  format: Lava-Test Test Definition 1.0
run:
  steps:
    - pwd
    - lava-sync start
    - sleep 14400
    - lava-sync fin
